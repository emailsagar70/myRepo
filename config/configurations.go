package config

const (
	//DBHost is the hostname of postgresDB
	DBHost = "postgresql.cqcvugskxi8t.us-east-2.rds.amazonaws.com"
	//DBUserName is the username of postgresql DB
	DBUserName = "postgres"
	//DBPassword is the password of postgresql DB
	DBPassword = "postgresql"
	//DBName is the name of the database
	DBName = "postgres"
	//DBPort is the port gor Postgres
	DBPort = 5432
	//Port is where our service is running
	Port = "8080"
)
